{
  "name": "envio-directo-web",
  "version": "1.0.0",
  "description": "Sitio web Envío Directo con Tailwind compilado",
  "scripts": {
    "build": "tailwindcss -i input.css -o styles.css --minify",
    "watch": "tailwindcss -i input.css -o styles.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.1"
  },
  "dependencies": {
    "imagemin-webp": "^8.0.0"
  }
}
